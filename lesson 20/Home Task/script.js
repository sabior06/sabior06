'use strict';

function fib(n) {
    return  n = n-1 + n-2;
  }
  
  alert( fib(2) ); 
  alert( fib(2) ); 
  alert( fib(2.5) );
  alert( fib(3) );
  alert( fib(4) );
  alert( fib(5.5) );
  alert( fib(8) );
  alert( fib(12) );
  

  var x = 1000;
  var y = 20 >= 2;
  var z = x / y;